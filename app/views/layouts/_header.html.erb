<nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
  <div class="container px-4 px-lg-5">
    <%= link_to t(".hotel_name"), root_path, class: "navbar-brand" %>
    <div class="collapse navbar-collapse" id="navbarResponsive">
      <ul class="navbar-nav ms-auto">
        <% if logged_in? %>
          <li class="nav-item"><%= link_to t(".register"), "#about", class: "nav-link" %></li>
          <li class="nav-item"><%= link_to t(".view"), rooms_path, class: "nav-link" %></li>
          <li class="dropdown nav-link">
            <%= link_to current_user.name, "#", class: "dropdown dropdown-toggle" %>
            <div class="dropdown-content">
              <%= link_to t(".profile"), "#" %>
              <%= link_to t(".settings"), edit_user_path(current_user) %>
              <%= button_to t(".log_out"), logout_path, method: :delete, class: "btn btn-danger" %>
            </div>
          </li>
        <% else %>
          <li class="nav-item"><%= link_to t(".login"), login_path, class: "nav-link" %></a></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
