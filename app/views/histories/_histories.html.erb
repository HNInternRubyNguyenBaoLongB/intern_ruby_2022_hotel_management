<tr>
  <td><%= histories.user_name %>
  <td><%= number_to_currency(change_money(histories.total_price)) %>
  <td><%= histories.bookings.count %>
  <td>
    <span class='<%= check_status_history(histories.status) %>'><%= histories.status %> </span>
  </td>
  <td>
    <%= link_to bookings_path(bill_id: histories.id) do %>
      <i class="fa-solid fa-eye"></i>
    <% end %>
  </td>
<tr>
